name: Test process-only prs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create label-actions config
        run: |
          mkdir -p .github
          cat > .github/label-actions.yml << 'EOF'
          enhancement:
            issues:
              comment: 'Enhancement requested on issue!'
            prs:
              comment: 'Enhancement requested on PR!'
          EOF

      - name: Run label-actions with process-only=prs
        id: action
        uses: .
        continue-on-error: true
        with:
          process-only: 'prs'
        env:
          GITHUB_EVENT_NAME: pull_request

      - name: Assert action completed (no crash)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "cancelled" ]; then
            echo "FAIL: action was cancelled unexpectedly"
            exit 1
          fi
          echo "PASS: action completed with outcome=$OUTCOME"
